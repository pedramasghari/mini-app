POSTGRES_USER=miniapp
POSTGRES_PASSWORD=change-me
POSTGRES_DB=miniapp

# Used by Next.js server-side rewrites. In Docker this MUST use the service name.
BACKEND_URL=http://backend:4000

# Public URL seen by the backend for CORS.
FRONTEND_URL=http://localhost
